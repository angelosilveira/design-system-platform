{
  "name": "@design-system/tokens",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'no lint configured yet'",
    "build": "echo 'tokens are consumed as TS source, no build step needed'"
  }
}
